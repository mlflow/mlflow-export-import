import logging

logging.basicConfig(
    #format = "%(asctime)s %(levelname)-7s %(message)s",
    format = "%(asctime)s %(levelname)s %(message)s",
    level = logging.INFO,
    datefmt = "%Y-%m-%d %H:%M:%S")
